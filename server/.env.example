# Copy to .env and set real passwords before first start.
API_PORT=8480
DB_NAME=kittencannon
DB_USER=kittencannon
DB_PASS=change-me
DB_ROOT_PASS=change-me-too
# Lock down to the game's origin later if desired; * is fine (no cookies/credentials used).
CORS_ALLOW_ORIGIN=*
